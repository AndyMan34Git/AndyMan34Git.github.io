if (typeof __pack == 'undefined') { let __pack }; __pack = prompt("Type the name of a pack (CASE SENSITIVE, NO SPACES)"); for (let i = 0; i < packs[__pack].length; i++) { result = packs[__pack][i]; if (!savedItems.some(item => item.name === result.name)) { savedItems.push({ name: result.name, rarity: result.rarity }); localStorage.setItem("savedItems", JSON.stringify(savedItems)); console.log(`New ${rarities[result.rarity]}: ${result.name}!`) } }